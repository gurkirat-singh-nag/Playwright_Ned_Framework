{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://qatalyst-replica.local/schemas/pipeline-state.schema.json",
  "title": "Pipeline State",
  "description": "Story-scoped checkpoint/resume state (pipeline-state.json), produced and consumed by .github/skills/pipeline-state/state.js.",
  "type": "object",
  "required": ["story", "status", "currentStage", "stages", "timestamps", "errors"],
  "additionalProperties": false,
  "properties": {
    "story": { "type": "string" },
    "status": { "type": "string", "enum": ["NOT_STARTED", "IN_PROGRESS", "COMPLETED", "FAILED", "BLOCKED"] },
    "currentStage": {
      "type": "string",
      "enum": ["framework-discovery", "test-plan", "reuse-check", "exploration", "test-cases", "test-script", "execution", "healing", "reporting"]
    },
    "stages": {
      "type": "object",
      "required": ["framework-discovery", "test-plan", "reuse-check", "exploration", "test-cases", "test-script", "execution", "healing", "reporting"],
      "additionalProperties": false,
      "properties": {
        "framework-discovery": { "$ref": "#/definitions/stageEntry" },
        "test-plan": { "$ref": "#/definitions/stageEntry" },
        "reuse-check": { "$ref": "#/definitions/stageEntry" },
        "exploration": { "$ref": "#/definitions/stageEntry" },
        "test-cases": { "$ref": "#/definitions/stageEntry" },
        "test-script": { "$ref": "#/definitions/stageEntry" },
        "execution": { "$ref": "#/definitions/stageEntry" },
        "healing": { "$ref": "#/definitions/stageEntry" },
        "reporting": { "$ref": "#/definitions/stageEntry" }
      }
    },
    "reuse": {
      "type": ["object", "null"],
      "additionalProperties": false,
      "properties": {
        "decision": { "type": "string" },
        "coverage": { "type": "number", "minimum": 0, "maximum": 100 }
      }
    },
    "timestamps": {
      "type": "object",
      "required": ["created"],
      "additionalProperties": true,
      "properties": {
        "created": { "type": "string" }
      }
    },
    "errors": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["stage", "at", "message"],
        "additionalProperties": false,
        "properties": {
          "stage": { "type": "string" },
          "at": { "type": "string" },
          "message": { "type": "string" }
        }
      }
    }
  },
  "definitions": {
    "stageEntry": {
      "type": "object",
      "required": ["status"],
      "additionalProperties": true,
      "properties": {
        "status": { "type": "string", "enum": ["NOT_STARTED", "IN_PROGRESS", "COMPLETED", "FAILED", "SKIPPED", "BLOCKED"] },
        "validatedAt": { "type": "string" },
        "inputHash": { "type": ["string", "null"] },
        "detail": { "type": "string" },
        "reason": { "type": "string" },
        "at": { "type": "string" },
        "invalidatedReason": { "type": "string" },
        "file": { "type": "string" }
      }
    }
  }
}
