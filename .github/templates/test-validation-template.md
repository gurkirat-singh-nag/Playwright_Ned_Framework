# test-validation.md - Artifact Contract

Produced by: Test Validator
Consumed by: The invoking agent's gate check (test-generator-ui), before capability discovery / generation may proceed.

This file is the validation verdict for one request's `test-design.json`. It is derived from `test-design.json`, `requirements.md`, `framework-profile.json`, the class index, and any other artifact already present for the slug (e.g. `testcases.json`, for integrity checking only). It never contains automation code and is never produced by launching a browser or MCP session.

---

## Status

<!-- One of: PASS, PASS_WITH_WARNINGS, BLOCKED. State blocker/warning/info counts. -->

## Acceptance Criteria Coverage

<!-- Table: AC-# | Status (covered/partial/uncovered) | Scenario(s) | Note -->

## Issues

<!-- Grouped by severity (BLOCKER, then WARNING, then INFO), then by category:
     artifact-integrity, requirement-consistency, test-data, duplicate,
     technology-consistency, existing-automation-awareness, automation-feasibility,
     scenario-quality, risk-review. Never silently repair or rewrite anything -
     report the problem and a recommendation only. -->

## Scenario Quality / Automation Feasibility / Risk Review

<!-- Findings not covered above, or an explicit "no findings" statement. -->

## Duplicate / Overlapping Scenarios

<!-- duplicate / probable duplicate / distinct, with reason. Never auto-deleted. -->

## Technology Consistency

<!-- Cross-check against framework-profile.json. -->

## Recommendations

<!-- Actionable next steps, ranked by what actually blocks progress. -->
