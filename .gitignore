node_modules/
auth.json
allure-report/
allure-results/
test-results/
# Playwright HTML reports
playwright-report/

# OS artifacts
.DS_Store
Thumbs.db

# Agentic pipeline artifacts - per-story working papers (test-design.json,
# exploration.md, screenshots, etc.) are local scratch, not shared. Anything
# meant to be committed (framework-profile.json, the reuse index) lives
# under index/ instead, which is tracked normally - no exception needed here.
artifacts/*


# MCP server runtime
.playwright-mcp/